// Add lightbox functionality and other scripts here
document.querySelectorAll('.lightbox').forEach(image => {
    image.addEventListener('click', () => {
        // Open image in lightbox
    });
});
